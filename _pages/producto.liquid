---
layout: default
pagination:
    data: editorial.editorial
    size: 1
    alias: producto
permalink: "catalogo/{{ producto.titulo | slug }}/"
---
<section class="py-10 flex items-center justify-center max-w-screen-lg mx-auto">
  <img class="w-56 py-10" src="/static/images/stubs/editorial-vampira.png" alt="">
</section>

<!-- Card -->
<section class="flex flex-col items-center py-12 pt-0 sm:flex-row max-w-screen-lg mx-auto gap-14">
  <!-- Imagen -->
  <img class="hidden w-2/3 sm:block" src="{{ producto.imagen }}" alt="">
  <!-- Descripcion -->
  <div class="flex flex-col justify-between gap-4 my-4">
    <!-- Top -->
    <div class="flex flex-col gap-4">
      <!-- Header -->
      <div class="border-b-2 border-blue-900">
        <h1 class="text-2xl uppercase sm:text-4xl">{{ producto.titulo }}</h1>
        <h3 class="text-xs text-blue-800 uppercase sm:text-base">
          {{ producto.autor }}{% if producto.instagram != ""%} - @{{ producto.instagram }}{% endif %}
        </h3>
      </div>
      <!-- ONLY MOBILE: Image -->
      <img class="pr-20 my-4 sm:hidden" src="{{ producto.imagen }}" alt="">
      <!-- Descripcion -->
      <p class="text-xs sm:pl-2">{{ producto.descripcion }}</p>
      <!-- Precio -->
      <h1 class="text-3xl text-blue-800 uppercase">${{ producto.precio }}</h1>
    </div>
    <!-- Bottom -->
    <div class="flex items-start justify-between">
      <div class="flex flex-col">
        <!-- Comprar -->
        <a class="p-2 text-lg uppercase border-2 sm:text-2xl flex items-center justify-center" href="{{ producto.url }}">
          <svg class="w-6 h-6 mr-2">
            <use xlink:href="/static/sprites/solid.svg#shopping-cart"></use>
          </svg>
          Comprar
        </a>
      </div>
      <!-- Compartir -->
      <div class="flex flex-col gap-2">
        <p class="text-xs text-right border-b-2 border-blue-900">Compartir</p>
        <div class="flex gap-2">
          <a href="#">
            <svg class="w-5 h-5">
              <use xlink:href="/static/sprites/brands.svg#instagram"></use>
            </svg>
          </a>
          <a href="#">
            <svg class="w-5 h-5">
              <use xlink:href="/static/sprites/brands.svg#spotify"></use>
            </svg>
          </a>
          <a href="#">
            <svg class="w-5 h-5">
              <use xlink:href="/static/sprites/brands.svg#twitter"></use>
            </svg>
          </a>
          <a href="#">
            <svg class="w-5 h-5">
              <use xlink:href="/static/sprites/brands.svg#facebook"></use>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
